import * as auth from './auth'
import * as db from './db'
import * as firebase from './firebase'

// create a single source of entry for all firebase related content
export {
  auth,
  db,
  firebase,
}
